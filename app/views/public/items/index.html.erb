<h2>商品一覧(全<%= @items_all.count %>件)</h2>

<% @items.each do |item| %>
<div>
  <%= link_to item_path(item.id) do %>
    <%= image_tag item.image %>
  <% end %>
  <%= item.name %>
  ￥<%= item.price %>
<% end %>
</div>

<%= paginate @items %>